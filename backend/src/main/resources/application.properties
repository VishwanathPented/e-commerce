# ===================================================================
# E-commerce Backend Configuration
# ===================================================================

# Server Configuration
server.port=8080
# server.servlet.context-path=/api

# Database Configuration (MySQL)
# Update 'username' and 'password' with your local MySQL credentials
spring.datasource.url=jdbc:mysql://localhost:3306/ecommerce_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=atpchz6aek
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# JWT Configuration (Secret Key usually goes in environment variables)
# Using a dummy secret for development: "SecretKeyForJwtSigningShouldBeLongAndSecure1234567890"
app.jwt.secret=SecretKeyForJwtSigningShouldBeLongAndSecure1234567890
app.jwt.expiration-ms=86400000

# Razorpay Configuration (Mock or Replace with Real Keys)
razorpay.key.id=rzp_test_YourKeyId
razorpay.key.secret=YourKeySecret

# Delhivery Configuration
delhivery.api.token=YourDelhiveryToken
delhivery.api.url=https://track.delhivery.com/api/v1/packages
